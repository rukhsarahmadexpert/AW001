<div class="row">
    <div class="col-xl-12">
        <div id="panel-1" class="panel">
            <div class="panel-hdr">
                @*<h2>
                        <a class="btn btn-md btn-primary" href="/Product/Create">
                            <i class="fal fa-plus-circle"></i>  Create New <span class="fw-300"><i>Product</i></span>
                        </a>
                    </h2>*@
            </div>
            <div class="panel-container show">
                <div class="panel-content">
                    <!--      <div class="panel-tag">
                                <p>Drivers Base Information</p>
                            </div>-->
                    <!-- datatable start -->
                    <table id="dt-basic-example1" class="table table-bordered table-hover table-striped w-100">
                        <thead class="bg-primary-600">
                            <tr>
                                <th>Assigned Date</th>
                                <th>Customer Name</th>
                                <th>Site Name</th>
                                <th>Order No</th>
                                <th>Requested Quantity</th>
                                <th>OrderProgress</th>
                                <th>Operater</th>
                                <th>Action</th>
                            </tr>
                        </thead>
                        <tbody></tbody>
                    </table>
                    <!-- datatable end -->
                </div>
            </div>
        </div>
    </div>
</div>

@section scripts{
    <script>

        $(document).ready(function () {

            $("#dt-basic-example1").DataTable({
                "paging": true,
                "responsive": true,
                "processing": true,
                "searching": true,
                "serverSide": true, // for process server side
                "filter": false, // this is for disable filter (search box)
                "orderMulti": false, // for disable multiple column at once
                "ajax": {
                    "url": "/CustomerOrder/SiteAssignedOrderAll",
                    "type": "POST",
                    "datatype": "json"
                },

                "columns": [
                    //{ "data": "Id", "name": "Id", "autoWidth": true },// key for edit delete
                    { "data": "CreatedDates", "Name": "CreatedDates", "autoWidth": true },
                    { "data": "CustomerName", "Name": "CustomerName", "autoWidth": true },
                    { "data": "SIteName", "name": "SIteName", "autoWidth": true },
                    { "data": "CustomerOrderId", "name": "CustomerOrderId", "autoWidth": true },
                    { "data": "RequestedQuantity", "name": "RequestedQuantity", "autoWidth": true },
                    { "data": "OrderProgress", "name": "OrderProgress", "autoWidth": true },
                    { "data": "UserName", "name": "UserName", "autoWidth": true },  
                    {
                        render: function (data, type, row) {
                            return '<div class="pull-right" title="View Order and Deliver"><button  class="btn btn-info btn-sm" data-id="Id" onclick="Details(' + row.Id + ')"><i class="fal fa-eye"></i></button>'
                        }
                    }
                ]
            });

        });


        function Details(Id) {
            alert(Id);
        }
    </script>
    }